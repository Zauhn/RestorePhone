{% extends '::base.html.twig' %}
{% block body %}
    <div class="well">

<h3>Liste des reparations</h3>

<!--<ul>

{% for recherche in liste_recherche %}

<li><a href="{{ path('StageRestorePhoneScriptreparation_voirReparation', {'id': recherche.id}) }}">{{ recherche.Client.nom }}</a></li>

{% else %}

<p>Désolé il n'y a, pour le moment, aucune reparation correspondant à votre recherche.</p>

{% endfor %}

</ul>
{% for reparation in liste_recherche %}
      <tr>
            <td>{{ reparation.getId() }}</td>
        </tr>
{% endfor %}-->

</div>
{% endblock %}